# سجل التغييرات والإصدارات (Changelog)

جميع التغييرات الملحوظة في مشروع **Daily Sticky (الملاحظة اليومية)** موثقة هنا.

الصيغة تتبع [Keep a Changelog](https://keepachangelog.com/en/1.0.0/)، ويلتزم هذا المشروع بقواعد [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [0.5.0] - 2026-09-27

### ✨ الميزات الأساسية (Features)
- **واجهة مهام يومية عربية كاملة (RTL)**:
  - دعم أصيل لاتجاه اليمين إلى اليسار (Right-to-Left).
  - خطوط عائلة ثمانية (Thmanyah) الرسمية المدمجة.
  - إدخال فوري للمهام، وتعديل وحذف عبر قائمة سياقية غنية.
- **أولويات المهام (Task Priorities)**:
  - دعم ثلاث مستويات أولوية (`عالية` 🔴، `متوسطة` 🟡، `منخفضة` 🟢).
  - تمييز بصري فوري مع فرز تلقائي منضبط.
- **الترحيل اليومي للمهام (Daily Rollover)**:
  - ترحيل تلقائي للمهام غير المكتملة عند منتصف الليل إلى اليوم الجديد دون فقدان البيانات.
  - الحفاظ على سجل الأيام السابقة كأرشيف تاريخي للقراءة فقط.
- **عرض التاريخ المزدوج والحكمة اليومية**:
  - عرض متزامن للتقويمين الميلادي والهجري.
  - حكمة يومية متجددة مستوردة من رصيد الحكم المدمج.
- **أرشيف وسجل المهام (History Window)**:
  - استعراض الأيام السابقة والمهام المرتبطة ونسب الإنجاز اليومية بدقة.
- **طبقة سطح المكتب الهادئة (Desktop Layer)**:
  - نافذة ملتصقة بسطح المكتب، مستبعدة من شريط المهام (Taskbar) ومبدّل النوافذ (Alt+Tab).
  - تكامل متقدم مع بيئة نظام ويندوز.
- **حفظ واستعادة أبعاد وموضع النافذة (Geometry Persistence)**:
  - حفظ الإحداثيات محلياً واستعادتها بأمان، مع دعم كامل لتعدد الشاشات (Multi-monitor) وتغيير الدقة.
- **صينية النظام (System Tray)**:
  - إظهار وإخفاء الملاحظة بنقرة واحدة، مع خيار الخروج النظيف.
- **التشغيل التلقائي مع ويندوز (Windows Startup)**:
  - خيار تشغيل التطبيق تلقائياً عند تسجيل الدخول عبر سجل النظام (HKCU Run) دون الحاجة لصلاحيات مدير النظام (Admin).
- **نافذة الإعدادات (Settings Dialog)**:
  - إمكانية تفعيل أو تعطيل خيار البدء مع ويندوز بيسر.
- **أدوات التحكم العلوية وشعار التطبيق**:
  - شريط أدوات علوي أنيق يضم زر الإعدادات (`⚙`)، وزر السجل (`📄`)، وزر الخروج (`✕`).
  - دمج الشعار الرسمي للتطبيق (`logo.png`) في النوافذ وصينية النظام والإعدادات.
- **حزمة التوزيع والمثبّت (Packaging & Inno Setup)**:
  - إعداد مواصفات PyInstaller لإنتاج نسخة مستقلة أحادية المجلد (`onedir`).
  - سكربت Inno Setup احترافي ينتج مثبت ويندوز نظيف (`DailySticky-Setup-0.5.0.exe`).
  - عزل بيانات المستخدم بالكامل في `%LOCALAPPDATA%\DailySticky\daily_sticky.db`.

### ⚠️ قيود معروفة مؤجلة (Known Limitations)
- **اختصار إظهار سطح المكتب (Win+D)**: يؤدي الضغط على Win+D إلى إخفاء الملاحظة مؤقتاً خلف سطح مكتب Explorer؛ يمكن استعادتها فوراً بالنقر على أيقونة صينية النظام أو النقر المباشر على سطح المكتب. هذا السلوك مؤجل ومدرج للإصدارات اللاحقة.
